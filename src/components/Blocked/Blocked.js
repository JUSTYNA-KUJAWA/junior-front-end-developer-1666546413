import React from "react";
import styles from "./Blocked.module.scss";

const Blocked = () => {
  return (
    <div className={styles.root}>
      <h1>You can not see the task</h1>
    </div>
  );
};

export default Blocked;
